@model Bes.Models.BesEntity.bankTable
@{
    ViewBag.Title = "Banka Düzenle";
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}

<div class="alert alert-warning alert-dismissible">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
    <h5><i class="icon fas fa-exclamation-triangle"></i> UYARI!!!!!</h5>
    <ul>
        <li>Tüm Alanlar Doldurulmalıdır</li>
    </ul>
</div>

<div class="card">
    <div class="card-body">
        @using (Html.BeginForm("EditBank", "Admin", FormMethod.Post))
        {
            @Html.AntiForgeryToken()
            <div class="row">
                <div class="col-md-6">
                    <div class="form-group">
                        @Html.TextBoxFor(m => m.bankID, new { @class = "form-control d-none" })
                        <label>Banka Kodu</label>
                        @Html.TextBoxFor(m => m.bankCode, new { @class = "form-control" })

                    </div>
                </div>
                <div class="col-md-6">
                    <div class="form-group">
                        <label>Banka Adı</label>
                        @Html.TextBoxFor(m => m.bankName, new { @class = "form-control" })

                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">

                    <div class="form-group">
                        <label>Açıklama</label>
                        @Html.TextAreaFor(m => m.bankDescription, new { @class = "form-control" })
                        @*<textarea class="form-control" rows="10" placeholder="Açıklama"></textarea>*@
                    </div>
                </div>
            </div>
            <div class="row">
                <div class="col-md-12">
                    <button type="submit" class="btn btn-outline-success float-right" style="width:125px;">
                        <i class="fas fa-edit"></i>&nbsp; Güncelle
                    </button>
                    <a href="@Url.Action("Index", "Admin")" type="reset" class="btn btn-outline-danger float-left" style="width:125px;">
                        <i class="fas fa-times-circle"></i>&nbsp; İptal Et
                    </a>
                </div>
            </div>
        }
    </div>
</div>